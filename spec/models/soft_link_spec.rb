require 'spec_helper'

describe SoftLink do
  #context "it should accept linkable_type_and_id" do
    #before do
      #@page = Factory(:page)
      #@linkable_type  = @page.class.base_class.name
      #@linkable_id    = @page.id
      #@linkable_type_and_id = "#{@linkable_type}_#{@linkable_id}"
      #@link= Factory.build(:soft_link, :linkable_type_and_id => @linkable_type_and_id)
    #end
    #it { @link.linkable_type.should == @linkable_type }
    #it { @link.linkable_id.should == @linkable_id }
    #it { @link.should be_valid }
    #it { @link.linkable.should == @page }
  #end
end
