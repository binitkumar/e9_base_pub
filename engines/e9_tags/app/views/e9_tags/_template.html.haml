%li
  %span.admin-tag
    %span= tag
    = link_to t(:remove_tag), 'javascript:;', :class => 'delete-tag', :title => t(:delete_tag_link_instructions, :scope => :e9_tags)
  = hidden_field_tag "#{f.object_name}[#{u_context}_tag_list][]", tag, :id => "#{f.object_name}_#{u_context}_tag_list", :class => 'tag-input'
