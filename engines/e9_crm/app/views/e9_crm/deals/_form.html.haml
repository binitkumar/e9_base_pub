= form_for resource, :url => polymorphic_path([parent, resource]), :remote => request.xhr? do |f|
  .errors= resource_error_messages!

  = render 'form_inner', :f => f

  .actions
    - if resource.lead?
      = f.submit_with_object_name('lead')
    - else
      = f.submit
