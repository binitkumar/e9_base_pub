<% if resource.errors.any? %>
  $('form .errors').html("<%= escape_javascript(resource_error_messages!) %>");
  $.fn.colorbox.resize();
<% else %>
  window.location = "<%= manage_admin_banner_url(resource) %>";
<% end %>
