- content_for :toolbar_right do
  = toolbar_actions do
    = link_to_new_resource(resource_class, :model => 'Text Snippet', :snippet => { :formatter => 1 })
    = link_to_new_resource(resource_class, :model => 'WYSIWG Snippet')

= render 'shared/admin/index', :collection => collection
