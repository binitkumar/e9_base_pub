!!! XML
%urlset{:xmlns => google_site_map_xmlns}
  - @results.each do |result|
    %url
      %loc= result.url
      %lastmod= I18n.l(result.updated_at.to_date, :format => :iso8601)
      %changefreq= changefreq(result)
      %priority= priority(result)
