<% if resource.errors.any? %>
$('#event_transaction_form .errors').html("<%= escape_javascript(resource_error_messages!) %>");
<% else %>
setTimeout(function(){ location.href = "<%= response.headers['Location'] %>"; } ,0);
<% end %>
