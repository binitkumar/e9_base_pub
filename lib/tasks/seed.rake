namespace :db do
  # NOTE currently lettings seeds load then loading env
  #
  #desc 'Load the seed data from db/seeds/[RAILS_ENV].rb'
  #task :seed => :environment do
    #seeds = File.join(Rails.root, 'db', 'seeds', "#{RAILS_ENV}.rb")
    #if File.exist?(seeds)
      #puts "Loading #{seeds}"
      #load seeds
    #else
      #puts "File #{seeds} not found"
    #end
  #end
end
